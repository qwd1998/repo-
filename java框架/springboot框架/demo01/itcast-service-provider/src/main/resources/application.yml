server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mybastis?serverTimezone=GMT%2B8
    username: root
    password: root
  application:
    name: service-provider  #作为微服务的名称

mybatis:
  type-aliases-package: cn.itcast.service.pojo

eureka:
  client:
    service-url:
      defaultZone: http://localhost:10086/eureka


